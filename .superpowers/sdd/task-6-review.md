REVIEW PACKAGE Task 6
BASE: 1f1dfeb1
HEAD: 9f87c6d0

COMMITS:
9f87c6d0 feat(template): add key-reasons checklist to Why section

STAT:
 src/lib/__tests__/template-design.test.ts | 6 ++++++  src/lib/ultimateTemplate.ts               | 3 +++  2 files changed, 9 insertions(+)

FULL DIFF:
diff --git a/src/lib/__tests__/template-design.test.ts b/src/lib/__tests__/template-design.test.ts index b50c0fe7..a77955e4 100644 --- a/src/lib/__tests__/template-design.test.ts +++ b/src/lib/__tests__/template-design.test.ts @@ -123,11 +123,17 @@ describe('Soft Evolution design invariants', () => {      expect(electricien).toContain('id="process"');      const coiffeur = generateUltimateSite({ ...lead, sector: 'Coiffeur' } as any, undefined);      expect(coiffeur).not.toContain('id="process"');    });      it('about section has a second paragraph and honest mini-stats', () => {      const html = build();      expect(html).toContain('about-mini');      expect(html).not.toContain('undefined');    }); + +  it('why section lists key reasons', () => { +    const html = build(); +    expect(html).toContain('why-list'); +    expect(html).toMatch(/class="[^"]*why-list-item[^"]*"/); +  });  }); diff --git a/src/lib/ultimateTemplate.ts b/src/lib/ultimateTemplate.ts index 349b5f9e..95a902d0 100644 --- a/src/lib/ultimateTemplate.ts +++ b/src/lib/ultimateTemplate.ts @@ -1651,20 +1651,23 @@ ${!hasBespokeProcess ? buildProcess(content, lang) : ''}          </div>      </section>        <section class="section-dark" id="why">          <div class="container">              <div class="why-grid">                  <div class="why-text reveal">                      <span class="section-label">${ui.eyebrowWhy}</span>                      <h2>${content.aboutTitle || (lang === 'en' ? 'Our Approach' : 'Notre Approche')}</h2>                      <p>${lang === 'en' ? `Our approach: ${pack.whyUs.slice(0, 2).map(w => w.title.toLowerCase()).join(' and ')}.` : `Notre approche : ${pack.whyUs.slice(0, 2).map(w => w.title.toLowerCase()).join(' et ')}.`}</p> +                    <ul class="why-list"> +                        ${pack.whyUs.slice(0, 3).map((w: any) => `<li class="why-list-item"><i data-lucide="check" width="16"></i> <span><strong>${w.title}</strong>  ${w.desc}</span></li>`).join('')} +                    </ul>                  </div>                  <div class="why-img reveal">                      <img src="${proxiedImg(getImg(2))}" ${imgErr(2)} alt="${companyName}" loading="lazy">                      <div class="why-img-badge"><div class="why-img-badge-num">${(hasRealRating && typeof rating === 'number' && rating > 0) ? rating + '/5' : (typeof establishedYear === 'number' && establishedYear > 0 ? (new Date().getFullYear() - establishedYear) + '+' : '24/7')}</div><div class="why-img-badge-text">${(hasRealRating && typeof rating === 'number' && rating > 0) ? (lang === 'en' ? 'Google Rating' : 'Note Google') : (typeof establishedYear === 'number' && establishedYear > 0 ? (lang === 'en' ? 'Years' : 'Ans') : (lang === 'en' ? 'Available' : 'Disponible'))}</div></div>                  </div>              </div>          </div>      </section>        <div class="stats" style="background:var(--primary)">
